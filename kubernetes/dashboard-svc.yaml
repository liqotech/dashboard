apiVersion: v1
kind: Service
metadata:
  labels:
    app: liqo-dashboard
  name: liqo-dashboard
spec:
  selector:
    app: liqo-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80